C = b + A[8]
C আছে $s1 এ, b $s2 তে, A এর base address s3

lw  $t0, 32($s3)    # মেমোরির Base Address ($s3) + 32 বাইট অফসেট থেকে A[8] এর মান $t0 রেজিস্টারে লোড করা হলো
add $s1, $s2, $t0    # b ($s2) এবং A[8] ($t0) যোগ করে চূড়ান্ত ফলাফল C ($s1) রেজিস্টারে রাখা হলো
